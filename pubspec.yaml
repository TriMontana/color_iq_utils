name: color_iq_utils
description: A pure-Dart engine for color science, color models, color spaces, etc.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.3.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  material_color_utilities: ^0.13.0
  # path: ^1.9.0

dependency_overrides:
  material_color_utilities: ^0.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
